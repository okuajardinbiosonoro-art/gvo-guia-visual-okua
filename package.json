{
  "name": "gvo-guia-visual-okua",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:server": "npm run build --workspace=packages/shared && npm run dev --workspace=apps/server",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/web && npm run build --workspace=apps/server",
    "typecheck": "npm run build --workspace=packages/shared && npm run typecheck --workspace=apps/web && npm run typecheck --workspace=apps/server"
  },
  "engines": {
    "node": ">=18"
  }
}
